"""Core cross-cutting utilities for the Maiden data pipeline.

Holds shared configuration, logging setup and the top-level build orchestration
that ties together ingest → parsers → cleaning → export → validation.
"""
