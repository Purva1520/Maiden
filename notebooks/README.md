# Notebooks

Exploratory Jupyter notebooks for the Python data pipeline (analysis,
prototyping, rating experiments in later phases).

Phase 0: empty. Notebooks are **research artifacts** and are kept separate from
the production game runtime (Principle 3: Research ≠ Runtime). They are excluded
from linting/formatting and are not part of any build.
